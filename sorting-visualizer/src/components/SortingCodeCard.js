import React from 'react';

function SortingCodeCard({ algorithm }) {
  // Placeholder content, replace with actual code display
  return (
    <div className="card">
      <h2>Sorting Algorithm Code</h2>
      <p>This is the code for {algorithm}.</p>
    </div>
  );
}

export default SortingCodeCard;
